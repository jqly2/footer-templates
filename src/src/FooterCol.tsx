type FooterProps = {
    name:string;
    url:string[];
}[];

const FooterCol = ([{name, url}]: FooterProps) => {
    return (
        <footer>
            <div>

            </div>
        </footer>
    )
}

export default FooterCol([]);